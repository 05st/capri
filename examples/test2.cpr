module main;

type Thing = {a: i64, b: bool, c: char};

fn main(): Thing {
    let mut test: Thing = {a = 1, b = false | {c = 'L'}};
    let empty = {};
    test.a;

    // These two are equivalent
    test.a = 2; 
    test = {a = 2 | {test - a}};
    test
};