module main;

import io;

fn even(n: i32)
    if n == 0
        true
    else
        odd(n - 1);

fn odd(n: i32)
    if n == 0
        false
    else
        even(n - 1);

fn main() {
    println(if even(20) "even" else "odd");
};
