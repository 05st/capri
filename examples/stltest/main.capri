module main;

import io;

fn id(x, b: i64, d) x;

fn rowPolymorphic(rec: {text: str | r}): str
    rec.text;

fn main() {
    print(id<str, char>(rowPolymorphic<{num: i64}>({text = "hello", num = 23}, 0, 'l')));
    print(id<str, bool>("test", 123, false));
};