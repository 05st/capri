module main;

import prelude;

fn id(x)
    x;
    
fn test(x, y) {
    if x {
        id(y)
    } else {
        y
    }
};

fn main() {
    print_i64(id(456));
    print_str(test(true, "!@#$%"));
    print_str(id("xyz\n"));
};