module main;

import io;

struct Example {
    field: str,
    xyz: bool
};

fn main() {
    mut var := Example {
        field = "test",
        xyz = false
    };
    var.field = "changed";
    println(var.field);
};
